# yaml-language-server: $schema=https://saturn-bot.readthedocs.io/en/latest/schemas/task.schema.json
name: saturn-bot Plugin Maven

plugins:
  - path: ./build/libs/saturn-bot-kotlin-gradle-plugin-standalone.jar
    configuration:
      message: "Hello Kotlin Gradle Plugin"
